# control-plane-seed-pins.txt — ADMISSIBLE frozen seed-tarball captures (re #469)
#
# Each non-comment line pins the sha256 of ONE audited seed tarball (the
# `td-builder seed-manifest` / `seed-unpack` capture of the pinned §5 native
# toolchain). `authenticate_seed_capture_db` refuses to type a TD_SEED_DB
# database AuditedSeed unless its `<db>.seed-tarball` sidecar names a sha256
# pinned here — so an unpinned capture fails closed, and pinning a new capture
# is a reviewed commit to this file, never a runtime side effect.
#
# Format: `<sha256-hex> <free-form label>` — one pin per line.
#
# No capture is pinned yet: the frozen-seed flow (tools/build-seed-tarball.sh)
# is operator-driven and no audited capture exists in-repo. Until one is
# audited and pinned, TD_SEED_DB intake fails closed by design.
