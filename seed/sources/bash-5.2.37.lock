# seed/sources/bash-5.2.37.lock — GNU bash 5.2.37, the shell the gate RUNNER itself executes
# every gate body under (builder/src/gates.rs runs each `script` as one `bash -c`). Built FROM
# SOURCE by the /td/store gcc 14.3.0, dynamic vs the /td/store glibc 2.41 (no guix bytes), so the
# ladder's own interpreter can come from td's store instead of the guix `guix shell` prelude
# (re #312). td-fetched UPSTREAM (ftp.gnu.org), NOT guix's repacked source. 5.2.37 is the version
# the pinned channel ships (tests/*-no-guix.lock: bash-5.2.37), so the /td/store bash matches it.
url https://ftp.gnu.org/gnu/bash/bash-5.2.37.tar.gz
sha256 9599b22ecd1d5787ad7d3b7bf0c59f312b3396d1e281175dd1f8a4014da621ff
file bash-5.2.37.tar.gz
