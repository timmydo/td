# td-tsgo.lock — the pinned tsgo tarball (the TypeScript 7 native compiler blob), warmed
# by td's OWN fetcher (move-off-Guile §5 consumer-swap, follow-on to td-fetch #116). The
# loop reads `path` (no `guix build -e '(@ (system td-ts) td-tsgo-tarball)'`); td-fetch is
# the FETCHER (tools/warm-tsgo.sh: fetch+verify, then the daemon add-to-store's the
# VERIFIED bytes — which lands at the SAME content-addressed FOD path the guix origin
# produces, so the CI image carries the identical path). The guix origin in
# system/td-ts.scm stays as the pin source-of-truth + oracle; the `tsgo-pin` gate asserts
# this lock matches it. Bump in lockstep with the origin (url + sha256 + path).
url https://registry.npmjs.org/@typescript/typescript-linux-x64/-/typescript-linux-x64-7.0.1-rc.tgz
sha256 4689c7e8d0fbee82a806dd100006242a96d24c76e4da5e678fcfe29012167d54
path /gnu/store/iy52hn6ws57mlb0xhxzrq5j5qasyz2mg-typescript-linux-x64-7.0.1-rc.tgz
