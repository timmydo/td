# td-txt conformance expectations overlay
#
# td-txt's known-gap manifest for the corpus in this directory (see spec/README).
# Kept out of the case files so the vendored GNU suites stay byte-for-byte
# pristine.
#
# GENERATED, not hand-curated, by examples/gen_expectations.rs: every case is run
# once through the built td-txt in the isolated harness and bucketed. A passing
# case is unlisted; every non-passing case is listed here so the gate stays green
# while td-txt grows. As td-txt improves, a listed case that starts passing reds
# the gate as an XPASS so it is promoted (removed here). Regenerate (do not
# hand-edit) on a pin bump or when passes change:
#   cargo build --release --manifest-path td-txt/Cargo.toml
#   cargo run --release --manifest-path td-txt/Cargo.toml --example gen_expectations \
#     -- td-txt/target/release/td-txt td-txt/spec > td-txt/spec/expectations.txt
#
# Format, one per line:   <xfail|skip> <corpus-file>::<case name>
#   xfail = td-txt runs it and gets the wrong answer today. It still RUNS every
#           gate; the failure is tolerated, a regression (an unlisted case that
#           fails) reds the gate, and an unexpected PASS (XPASS) reds it so the
#           entry is promoted.
#   skip  = not run at all, because the case TIMED OUT: it would cost the shared
#           gate the full per-case timeout for an answer already known to be
#           wrong.
xfail grep-cli.test.txt::-w over sliding greedy repeats on a long line
xfail grep-cli.test.txt::-o -w prints a shrunk span only at the first search position
xfail grep-cli.test.txt::-o -w skips two shrinkable spans in the middle of the line
xfail grep-cli.test.txt::-o -w after a shrunk span at the line start
xfail grep-cli.test.txt::a dollar before a bare pipe anchors in grep, where the pipe is a literal
xfail grep-cli.test.txt::so the text it looks like it wants is what it does NOT select
xfail grep-cli.test.txt::and the same at the start of a pattern
xfail grep-cli.test.txt::grep refuses a quantified assertion INSIDE a group, which td accepts
xfail grep-cli.test.txt::an anchor spells it the same way
xfail grep-cli.test.txt::and a word boundary too
xfail grep-cli.test.txt::a fuzz composite that diverges where each of its parts agrees
xfail grep-cli.test.txt::and the dropped interval does not make its group participate
xfail grep-cli.test.txt::a second fuzz composite in the same family
xfail grep-cli.test.txt::a BACKREFERENCE switches grep to glibc's reading of the same pattern
xfail grep-cli.test.txt::so the operator is a literal there, exactly as sed reads it
xfail grep-cli.test.txt::a dropped token immediately before a paren is what grep refuses
xfail grep-cli.test.txt::grep drops a leading interval but the NEXT one then has something to repeat
xfail grep-cli.test.txt::and an empty brace after an anchor is the literal it is at a branch start
xfail grep-cli.test.txt::as it is after a literal brace
xfail sed-cli.test.txt::without -s a failure on the FIRST operand outranks the quit code too
xfail sed-cli.test.txt::and q5 reports it rather than 5
xfail sed-cli.test.txt::q stops before a later operand is opened, so it is never reported
xfail sed-cli.test.txt::GNU's own r cannot see what its w has written, because the write is buffered
xfail sed-cli.test.txt::a --sandbox that FOLLOWS the w it would refuse comes too late in GNU
xfail sed-cli.test.txt::and a w naming the -f script truncates it before GNU reads it
xfail sed-cli.test.txt::F names the file GNU's $ lookahead has already opened
xfail sed-cli.test.txt::\c at the end of a text controls the text's own terminator
xfail sed-cli.test.txt::a dollar before a bare pipe is not the literal it looks like
xfail sed-cli.test.txt::but a MANDATORY single-byte matcher after the pair matches nothing at all
xfail sed-cli.test.txt::which a bracket spells the same way
xfail sed-cli.test.txt::a [ consumed as a closer test leaves the reader somewhere unmodelled
xfail sed-cli.test.txt::and the same shape after an earlier sub-expression
xfail sed-cli.test.txt::the same with a class
xfail sed-cli.test.txt::and with an equivalence class
