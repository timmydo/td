# Per-rung scratch dirs (`rootless`: staged store + DB snapshot; `oci-load`:
# OCI layouts + corrupted-archive control); kept on a red run for triage,
# removed on green.
.rootless-scratch/
.oci-load-scratch/
