section: side
status: claimed
handle: claude-opus-8c46a5
date: 2026-06-28
title: corpus-store-native
notes: plan/corpus-store-native.md
summary: More REAL corpus packages built by td's OWN /td/store toolchain via the engine — extending the BRICK 8 pattern ([[toolchain-subst]]'s bootstrap-hello-corpus-store-native, gate 414) past GNU hello to the rest of the corpus (sed, grep, make, tar, gzip, coreutils, bash…; split across agents). This is task 4 of the post-glibc-2.41 plan — "more corpus on the /td/store toolchain" — done the converged way: NOT the mesboot build-wrapper, but `td-builder build-recipe` with the full MODERN /td/store toolchain (gcc 14.3.0 + binutils 2.44 + glibc 2.41, all from the 229-byte seed) SUBSTITUTED for guix's gcc-toolchain-15.2.0 in the package's `-no-guix.lock`. Each gate: build the toolchain from seed, assemble a guix-gcc-toolchain-shaped /td/store toolchain (wrapper gcc/g++ + binutils + interp-rewritten bins), substitute it into the package lock, then build the package at the EXACT guix corpus version via the existing recipe-<pkg>.ts; the binary links the /td/store glibc 2.41, references NO guix gcc-toolchain, and runs in a store-ns own-root with /gnu/store ABSENT, doing real work. DURABLE (no guix oracle): pinned-input, no-guix, no-guix-toolchain (no guix gcc-toolchain ref in the binary), behavioral, structural. Inc 1 (this PR): GNU sed 4.9 (the version sed-no-guix.lock builds with guix's gcc-toolchain) — gate 416 `bootstrap-sed-corpus-store-native` + a leaf affected-checks case; the own-root sed substitutes foo→bar. Reuses the proven BRICK 8 toolchain build + recipe-sed.ts (already authored) + sed-no-guix.lock (already pinned) — no new lock, no engine change. Parallel-safe: new gate file + a leaf affected-checks case; no edit to system/td.scm / check.sh / Makefile / the toolchain scripts / the engine. Ladder in plan/corpus-store-native.md.
