section: side
status: done
handle: claude-opus-8c46a5
date: 2026-06-29
title: corpus-store-native
notes: plan/corpus-store-native.md
summary: DONE (Inc 1, PR #203) — more REAL corpus packages built by td's OWN /td/store toolchain via the engine, extending the BRICK 8 pattern ([[toolchain-subst]]'s bootstrap-hello-corpus-store-native, gate 414) past GNU hello. Task 4 of the post-glibc-2.41 plan — "more corpus on the /td/store toolchain" — done the converged way (NOT the mesboot build-wrapper): `td-builder build-recipe` with the full MODERN /td/store toolchain (gcc 14.3.0 + binutils 2.44 + glibc 2.41 from the 229-byte seed) SUBSTITUTED for guix's gcc-toolchain-15.2.0 in the package's `-no-guix.lock`. Inc 1 LANDED: GNU sed 4.9 — gate 416 `bootstrap-sed-corpus-store-native`; from seed the /td/store toolchain build-recipe-builds sed 4.9 (no guix gcc-toolchain ref) and the own-root sed substitutes foo→bar, /gnu/store ABSENT (validated from seed). PR #203 ALSO: (a) extracted the ~850-line chain into the SHARED lib `tests/bootstrap-chain.sh` (`bootstrap_modern_toolchain`) — the sed gate is 186 lines; the other 9 bootstrap-*-store-native gates can migrate (each needs a from-seed re-validation); (b) fixed a pre-existing from-seed bug ([[td-elf-interp-tmpdir-bug]]: elf-set-interp can't grow PT_INTERP, check.sh's short /tmp TMPDIR → ld keeps a dead build-dir interp → "cannot create executables"; pinned a long TMPDIR) that also affects the landed hello-corpus gate. FOLLOW-UPS (any agent, thin via the shared lib): grep/tar/gzip/make/coreutils/bash corpus gates; migrate the 9 inline gates to bootstrap-chain.sh; elf.rs PT_INTERP grow (removes the TMPDIR-length dependency). Ladder in plan/corpus-store-native.md.
