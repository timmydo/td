section: mainline
status: claimed
handle: claude-fable-65585b
date: 2026-06-23
title: crate-guix-free
summary: Make rust crate provisioning GUIX-FREE — no guix-daemon, no guix process, no new guix bytes, and NO guix differential oracle (human 2026-06-23 — "I don't want any new guix dependencies, even an oracle"). Today rust gates realize `.crate` deps via `guix build /gnu/store/<hash>.crate` (guix-daemon FOD fetch) and pass those /gnu/store paths as TD_VENDOR_CRATES. Target: td fetches each crate from static.crates.io with td-feed (pinned by the upstream Cargo.lock checksum), places the crate set as ONE content-addressed vendor TREE in a td-owned store via `store-add-recursive` (guix-free, the store_db family — NOT the daemon-client `store-add`), and `run_rust` vendors from that interned dir (TD_VENDOR_DIR). Crates are content-addressed, so directive-4 equivalence is the sha256 PIN (Cargo.lock checksum = upstream crates.io hash, NOT a guix artifact) — the guix differential is REDUNDANT and dropped. Durable guix-free legs: supply-chain (fetched sha == Cargo.lock pin), behavioral (builds a working binary), intrinsic-repro (td-builder check), structural (guix off PATH, td store, no daemon, no `guix build` in the gate). PoC on td-fetch's OWN 73-crate closure; the rust/gcc toolchain seed stays /gnu/store (pre-existing, retired last by [[td-source-bootstrap-decision]]). Then scale to the corpus rust locks + drop their /gnu/store crate strings (content-addressed name+sha). builder/src changes validate on the check-engine smoke tier. Brick ladder in plan/crate-guix-free.md.
