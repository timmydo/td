section: side
status: claimed
handle: claude-opus-d5df49
date: 2026-06-27
title: x86_64-toolchain
notes: plan/x86_64-toolchain.md
summary: Cross the full-source bootstrap UP from i686 to a native x86_64 toolchain at /td/store — the rung the i686 chain always anticipated ("Built i686 ... gcc later cross-builds to 64-bit", bootstrap-mescc.sh). The whole existing /td/store toolchain (gcc 14.3.0 + binutils 2.44 + glibc 2.41, #197/#193/#199) is i686/32-bit (ld-linux.so.2, --host=i686 --disable-multilib), but the upstream Rust pin is x86_64, so the rust-store-native (#196) runtime leg is blocked on ARCHITECTURE, not just glibc>=2.17 (the #196 track note + memory were wrong on that point — corrected here). Human-directed 2026-06-27 ("x86_64 toolchain first"): build the x86_64 rungs so the x86_64 rust runtime leg + userland (procs/fd/ripgrep) can run from /td/store. Ladder (guix-style cross-then-native, built BY the existing i686 gcc 14.3.0): cross binutils 2.44 (--target=x86_64) -> cross gcc 14.3.0 stage1 (C, no libc) -> x86_64 kernel headers (host warm-prep) -> x86_64 glibc 2.41 (built by the cross-cc; ld-linux-x86-64.so.2 + libc.so.6) -> cross gcc 14.3.0 stage2 (c,c++ + SHARED libgcc_s.so.1, needed by rustc) -> [rung X1 milestone] an x86_64 C AND C++ program links vs the /td/store x86_64 glibc 2.41 and RUNS in the store-ns own-root -> 42, /gnu/store ABSENT; then [rung X2] a NATIVE x86_64 gcc (gcc binary itself x86_64); then the rust-store-native x86_64 runtime + userland flip green. New self-contained gate + script (the lane's superset convention) + x86_64 kernel-headers warm; NO edit to system/td.scm / check.sh / Makefile / the i686 lane scripts (parallel-safe vs source-bootstrap mainline). Ladder + verified-red plan in plan/x86_64-toolchain.md.
