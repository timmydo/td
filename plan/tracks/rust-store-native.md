section: side
status: claimed
handle: claude-opus-5cd532
date: 2026-06-26
title: rust-store-native
notes: plan/rust-store-native.md
summary: Toward a usable, guix-free Rust userspace WITHOUT the guix system declaration (system/td.scm) — human-directed 2026-06-26. Replace the guix-built rust seed with the UPSTREAM Rust release tarball (static.rust-lang.org, sha256-pinned, td-fetch'd guix-free — NOT a guix artifact, so zero guix bytes), RELINKED to /td/store: the build recipe is "relink a binary tarball" (patch interp+RUNPATH so rustc/cargo run against the /td/store glibc, retarget rustc's C linker so it PRODUCES /td/store-linked binaries). Then assemble the Rust tools into a td-native profile (td-builder profile) run in a store-ns own-root with /gnu/store ABSENT — no guix operating-system, the replacement that lets the .scm system path retire. "Relocate now, source-rebuild later" (human) but the "now" bytes are upstream not a guix seed, so the guix part is eliminated from the start. DEPENDENCY: running upstream rustc from /td/store needs glibc>=2.17 (rustc std uses __cxa_thread_atexit_impl, added 2.17); the current /td/store glibc is 2.16.0, so the /td/store-RUNTIME behavioral leg is BLOCKED on the gcc agent's unbuilt glibc-final ([[source-bootstrap]]). Per human 2026-06-26: build the fetch+relink recipe + durable STRUCTURAL (no /gnu/store bytes, interp/RUNPATH->/td/store, closure-complete) + SUPPLY-CHAIN (sha==upstream pin, upstream-not-guix provenance) legs NOW; mark the /td/store-runtime leg PENDING glibc-final (not faked) — it flips green with a target swap when glibc-final lands. STATUS (2026-06-26): pin = rust 1.96.0 (latest stable) in seed/sources/rust-1.96.0.lock — manifest-warmed by warm-bootstrap-sources.sh, NO check.sh edit; td's OWN ELF PT_INTERP rewriter built in builder/src/elf.rs (`elf-set-interp`/`elf-interp`, NO patchelf — human: don't add a guix dep; RUNPATH is already `$ORIGIN/../lib` so ONLY interp needs retargeting); the rust-store-native gate (mk/gates/410) relinks the upstream rustc/cargo to /td/store + interns guix-free (store-add-recursive) + asserts the structural/supply-chain/provenance legs. Parallel-safe: new gate file + seed/sources lock, no spine edit; does NOT touch the gcc lane's bootstrap scripts or system/td.scm. Ladder in plan/rust-store-native.md. UPDATE (2026-06-28): the /td/store-RUNTIME leg is GREEN from-seed — the "target swap" was actually an ARCH correction (the /td/store toolchain was i686, the rust pin x86_64), so the new gate mk/gates/416 REUSES the x86_64 cross rungs (#201) via a one-line TD_X86_64_LIB lib-only guard, builds x86_64 zlib from source (libLLVM's libz), relinks + interns the upstream Rust 1.96.0 at /td/store, and RUNS rustc -vV + cargo --version in the store-ns own-root → rustc/cargo 1.96.0, /gnu/store ABSENT (verified-red: drop libz → own-root load fails). Track stays claimed for rung 3 (store-native rust COMPILE) + rung 4 (rust userland profile) + wiring the x86_64 toolchain as a fetchable substitute so those rungs fetch instead of rebuild.
