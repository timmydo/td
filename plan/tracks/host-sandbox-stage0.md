section: side
status: claimed
handle: claude-opus-5354e1
date: 2026-06-28
title: host-sandbox-stage0
notes: plan/host-sandbox-stage0.md
summary: North-Star — get the loop's harness off guix so ci/daily-full-suite.sh runs on a cloud VM with NO guix INSTALLED (human 2026-06-28, path B). Inc1 LANDED (#134 rust-seed gate: td builds its Rust engine td-builder from a frozen rust seed, guix off PATH). Inc2 (this re-take) is the spine: provision the WHOLE loop harness — td-builder + the sandbox toolchain (make/bash/coreutils/…) + the store — from a guix-CAPTURED-but-pinned seed tarball (human picked the seed-tarball mechanism 2026-06-28: guix process/daemon gone = North-Star priority 1/2; guix BYTES in loop scaffolding remain = retired-last priority 3). The old inc2 blocker (rust-free fast CI image can't compile stage0) is DISSOLVED by the daily-VM framing: the swap is guix-OPTIONAL — when guix is present (fast CI image) keep today's path untouched; when guix is ABSENT (the VM) provision from the unpacked seed. Increment 2a (first, additive gate `harness-seed`): prove the loop CONTAINER stands up from the seed ALONE — host /gnu/store + /var/guix NOT bound, guix off PATH, seed store bound at /gnu/store — and a real guix-free command runs inside (closes rust-seed's gap: it ran on a guix host where /gnu/store was present). Needs a small builder flag (`host-sandbox --store-from DIR --no-daemon`). 2b: check.sh reads the pin from channels.scm (not `guix describe`) + builds td-builder from the seed when guix absent. 2c: a guix-free `check-noguix` tier + daily-full-suite wiring. Exclusive landings on check.sh sequenced last.
