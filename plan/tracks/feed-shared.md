section: side
status: done
handle: claude-fable-65585b
date: 2026-06-23
title: feed-shared
notes: plan/feed-shared.md
summary: Wire td-feed (landed #157) into the loop as a SHARED, persistent, offline mirror for td-NATIVE fetch egress (the tsgo tarball + the bootstrap source tarballs that td-fetch / tools/warm-*.sh request) so many agents on different worktrees share ONE feed and the td-native downloads happen once. Decisions (human, 2026-06-23): (1) keep today's scope — the feed serves only td-native fetchers; the guix DAEMON keeps fetching its own crate/tarball FODs (the 1838 crates in tests/td-feed.index stay catalog-only, daemon-served, NOT re-warmed by the feed); (2) a PERSISTENT host td-feed serve daemon + a shared store at ~/.td/feed (TD_FEED_DIR), fixed loopback addr, started-if-not-running by tools/feed-ensure.sh (flock + addr/pid discovery); (3) adding artifacts = regenerate the committed index (gen-feed-index.sh) + an idempotent PREP warm of the delta into the shared store (no runtime config mutation); (4) check.sh's host prelude AUTO-warms the td-native subset into the shared feed (idempotent, warm = no-op) then exports TD_FEED_BASE so the offline loop's td-fetch reads from the shared feed. Design: a SIDECAR-based serve (store/<path> + store/<path>.sha256) makes the daemon index-free and self-describing, so a growing union across branches serves automatically — warm is the supply-chain gate (verify vs the pinned index), serve is the integrity gate (verify vs the sidecar). Inc1 = the mechanism (sidecar serve/warm + feed-ensure) + a cross-worktree-sharing gate, no spine edit. Inc2 = the check.sh prelude wiring + bootstrap sources in the index (exclusive landing, full loop). Brick ladder in plan/feed-shared.md.
