section: side
status: claimed
title: toolchain-subst-default
handle: claude-opus-0b4464
date: 2026-06-28
notes: plan/toolchain-subst-default.md
summary: Make the /td/store toolchain substitute the DEFAULT for the loop (human, 2026-06-28, "loop substitutes too") — so a toolchain gate FETCHES the lock-keyed signed toolchain instead of rebuilding the ~18-rung from-seed chain (~90 min). Builds on [[td-toolchain-input-addressed]] (2a, the stable key) and [[toolchain-subst]] (2b/2c, the lock-keyed publish->fetch leg in gate 358, which DEFERRED the loop wiring + the real-bytes publish). The genuinely new pieces vs gate 358 (which uses ephemeral served dirs + ephemeral keys per run): (1) a PERSISTENT signed substitute store keyed by tests/td-toolchain.lock; (2) a PINNED trust-anchor ed25519 public key the loop verifies against (private half = host/daily-runner secret, never in-repo); (3) a consumer-default RESOLVER the bootstrap gate sources — compute toolchain-path, if the persistent store has it and it verifies (sig + StorePath == lock-computed path + NarHash) serve-loopback+fetch+restore and USE it, else FALL BACK to the from-seed build; (4) a PUBLISHER the daily suite runs after its authoritative from-seed build (the gate-412 store-add-recursive -> store-add-input-addressed + subst-export swap). DELIBERATE directive-1 relaxation, human-approved + surfaced in the PR: the per-PR/local loop no longer builds the toolchain from source; ci/daily-full-suite.sh on fresh main is the SOLE remaining from-seed authoritative build + the publisher. Granularity = whole-toolchain (the lock already keys the whole toolchain). Trust = signature + input-addressed name (the toolchain is not byte-reproducible; repro-equality is task 3). Validate the resolver+fallback end-to-end with the real subst machinery on a fixture (the #207 pattern); the literal ~90-min from-seed publish runs in the daily suite (cold sources off the per-PR path).
