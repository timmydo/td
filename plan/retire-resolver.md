# plan/retire-resolver.md — retire input resolution off Guile (move-off-Guile §5)

Track: **retire-resolver**. The input-resolution LOCK (tests/td-build-inputs.lock,
NAME→store-path) is consumed by `td-builder resolve` (no Guile, the input-resolution
track) but GENERATED by Guile (tests/resolve-lock.scm → `specification->package`).
This track moves the GENERATION to td's own recipes, package-by-package; the toolchain
(the floor of the dependency closure) is retired LAST (§5). Handle: claude-fable-2715d4.

Once #68 retired td-recipe.scm (byte-identity), td's recipes lower via td-build (own
builder) to DISTINCT paths — so a recipe-sourced lock entry diverges from
specification->package's. That divergence is the point ("own, then diverge"): the
resolved deps become td's own builds, and the closure becomes td's, bottoming out at
the toolchain.

## Increment 1 (PR #73 — td-resolve-recipe): a recipe-sourced lock entry

Gate `td-resolve-recipe` (375): gettext-minimal's lock entry is generated from td's
recipe — recipe-gettext-minimal.ts lowered via system/td-build to its out path (drv
builder = td-builder), NO specification->package for the name. `td-builder resolve`
returns that td-recipe path. SELF-DISCRIMINATION: it diverges from
specification->package's path (own, then diverge — genuinely td's recipe). MIGRATION
ORACLE: ncurses (no td recipe yet) still resolves via Guile — the package-by-package
boundary. Additive: the pinned lock + nano's build untouched. The resolved gettext is
td's own (built + functioning per td-build-gettext 350 / td-realize-store 365).

## Next

- A generator that builds the WHOLE lock from recipes (td-build for names with a
  recipe; specification->package fallback for the rest), emitting a td-sourced lock.
- Swap nano's resolved build (td-build-resolved 260) onto the td-sourced lock — nano
  then links td's gettext (distinct path; the differential becomes durable, not
  byte-identical).
- Reconstruct more deps (ncurses, …) → more of the lock is td-sourced.
- Toolchain retired LAST (§5).
