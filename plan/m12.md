# Track: m12 — signed distribution (mainline)

**Claim status:** see `PLAN.md` (the single source of truth for claims).
**Origin:** mainline successor to M11; blocked on M11.
**Scope authority:** DESIGN §7.1 (approved roadmap — no further sign-off needed).

## Goal

A generation image is pushed to and pulled from a registry (local/offline inside
the loop), its signature verified before placement; the placer rejects unsigned
or tampered images.

## Acceptance (DESIGN §7.1)

Push, pull, verify-then-place green end-to-end inside the offline loop; the
placer rejects unsigned and tampered images (verified-red).

## Pre-decisions (DESIGN §2.7 — settled 2026-06-10, on paper, do not re-litigate)

- **Identity = digest of the distributed artifact** in its canonical form
  (today: sha256 of the docker-archive tarball; the OCI manifest digest once
  the `oci-load` track lands the canonical layout — a `DIGESTS.md`
  re-baseline). Signatures are over the digest, never the install ordinal N;
  "verify before placement" means digest first, slot second.
- **Signing vehicle: detached ed25519 signature over the digest; no sigstore**
  (cosign is Go-heavy and network-assuming — likely the next podman).
  Candidates: signify/minisign-style tool, or guile-gcrypt's ed25519 (already
  a build dependency of `system/td-generation.scm`). State the M8-style probe
  criterion here BEFORE adopting: offline-buildable from the pinned channel,
  sane derivation count.
- **Registry: a static layout, not a product.** A static `oci-layout`
  directory plus a trivial local server (or plain file transport) IS the
  registry inside the loop; no registry product enters the loop.
- **Downgrade policy: anti-rollback is out of scope.** Reject exactly three
  things: unsigned, bad signature, digest mismatch. An old but validly signed
  generation is re-placeable by design (manual rollback is the model).

## Open question (decide here when the track starts — §2.7/§6)

- **Key distribution**: how the target gets the verifying public key — placer
  flag, well-known path on `td-state`, or baked into the placer build.

## Carried-over to-do (from M10.3 — §2.7)

- **Placed `image-digest=` line**: the placer additionally writes
  `image-digest=sha256:…` into `boot/td/gen-N/td-identity`, computed over the
  artifact it actually unpacked. M10.3 landed WITHOUT this one-line increment,
  so it falls to M12's start; it is M12's verification anchor (verify the
  signature over the digest, hash the pulled artifact, compare, only then
  place — the placed record then states the verified identity).

## Constraints

- Offline loop, as always (FSDG relaxed to a non-goal 2026-06-11 — DESIGN §5);
  the probe-before-adopt discipline above is the hard gate on any new tool.
- Placer changes strengthen `place-check`; new rungs touch shared-spine
  `Makefile`/`check.sh`: standalone commits (§7.3).

## Working state

**Claimed 2026-06-11 (claude-fable-c4148a), draft PR #7.**

### Decisions at track start

- **Key distribution (the named open question): placer flag (`--pubkey FILE`).**
  Explicit and hermetic — the test passes the key like any other input; no hidden
  machine state. Rejected: baked-into-placer (the placer is a plain POSIX script;
  embedding key material couples rotation to a placer change) and td-state
  well-known path (invents a §2.6 allowlist entry before any deployment needs
  it — can layer on later as a convention for WHERE the flag's file comes from,
  without changing the placer contract).
- **Digest today = sha256 of the docker-archive tarball** (§2.7 staging; the
  placer hashes the artifact it actually unpacked). Moving identity to the OCI
  manifest digest when distribution adopts the oci-load canonical layout is a
  recorded representation change (DIGESTS.md re-baseline), not a convention
  change — revisit at S3.

### Sub-task ladder

- **S1 — placed `image-digest=` line (the §2.7 carried-over anchor).** The placer
  appends `image-digest=sha256:<hex>` (computed over `--image`) to the placed
  `boot/td/gen-N/td-identity`. Test first: `place-check` asserts the line is a
  well-formed sha256 and — when the rung passes the artifact paths (new
  `TD_IMAGES` env, wired in the `place` rung via `place-drv` printing the image
  store paths) — that it EQUALS the artifact's actual sha256. Verified-red:
  (A) assertion before implementation (line missing), (B) placer defect hashing
  the wrong content (manifest.json instead of the artifact) caught by the
  equality leg.
- **S2 — signing vehicle probe (M8-style, criterion before adoption).**
  Candidates: `signify` / `minisign` from the pinned channel (placer-side verify
  needs a tool on the placer's PATH — same pattern as veritysetup for --mkfs);
  guile-gcrypt ed25519 for the SIGNING side if convenient (already a dep).
  Criterion: offline-buildable from the pinned channel, sane derivation count,
  usable from the guix-free placer sandbox. Probe, decide, record here.
- **S3 — push/pull through a static registry layout.** Push = populate a static
  layout (file transport; no registry product) with the artifact + detached
  signature over its digest; pull = retrieve by digest; pulled artifact
  byte-identical to pushed. **DECIDED 2026-06-11: the layout is the canonical
  OCI layout** — the §2.7 representation move to manifest-digest identity is
  ALREADY recorded in DIGESTS.md (oci-load, 2026-06-11: "the form M12 signs"),
  so docker-archive + sidecar would re-litigate a settled, recorded decision.
  Shape: one skopeo-produced OCI layout (shared `blobs/sha256` store, one ref
  per generation) + a `signatures/` sidecar: per image a one-line digest
  STATEMENT file (`sha256:<hex>`) and its signify detached signature by the
  committed test key. Pull-by-digest = manifest blob at `blobs/sha256/<hex>` →
  config/layer blobs it references; "byte-identical" holds by content
  addressing (every pulled blob re-hashes to its name — asserted). New
  `registry` rung (HEAVY pool; Makefile exclusive landing): build the registry
  derivation (skopeo + signify as declared inputs), `--check` it, assert
  statement == skopeo-inspected manifest digest, signify -V green, full blob
  re-hash walk, pull walk; negative controls IN the rung on copies: unsigned
  (sig removed) rejected, tampered blob caught by the walk, forged statement
  fails signature verification. S1's docker-archive-sha256 placed line and
  TD_IMAGES equality stay AS IS until S4 moves placement input to the verified
  layout — then the placed `image-digest=` value becomes the manifest digest
  (the §2.7 representation change at the placer, recorded when it happens; the
  assertion stays value-equality against the actually-placed artifact, equally
  strong).
  Open (decide at S4): whether the registry rung should ALSO pin the gen-1
  statement to DIGESTS.md's recorded §2.7 identity (makes the registry the
  enforcement point for deliberate-only identity drift; brittle if other
  tracks legitimately move the image digest).
- **S4 — verify-then-place.** The placer (--pubkey) verifies the detached
  signature over the digest, hashes the pulled artifact, compares, and only then
  places; rejects exactly: unsigned, bad signature, digest mismatch
  (verified-red ×3 — the §7.1 acceptance). Anti-rollback explicitly out of
  scope (§2.7). **Design (settled at S4 start, 2026-06-12):**
  - The placer grows a VERIFIED input mode: `--registry DIR --digest
    sha256:HEX --pubkey FILE` (mutually exclusive with `--image`). Before any
    staging it enforces the same pull contract tests/registry-check.sh's
    verify_pull states (statement exists for the requested digest, signify
    signature verifies, statement content == digest, manifest blob re-hashes
    to it, every referenced blob present + re-hashes), all with base tools
    (sha256sum, gzip) plus signify on PATH — the same pattern as veritysetup
    for --mkfs. Only then are the layer blobs decompressed into the work dir
    and handed to the EXISTING placement path (embedded-identity binding,
    staging, mkfs/verity, menu) unchanged.
  - Placed `image-digest=` value in verified mode = the verified MANIFEST
    digest (the §2.7 representation move at the placer); legacy `--image`
    mode keeps the artifact sha256. place-check's TD_IMAGES equality gains
    the digest form ("N=sha256:HEX" expected value) alongside the path form —
    an extension, not a weakening: still value-equality against what was
    actually placed.
  - **Differential anchor:** placing gen-1 from the verified registry and
    directly from the docker-archive must yield IDENTICAL placed trees except
    the image-digest value (gzip round-trips losslessly, so the applied layer
    bytes are provably the same; the oracle is the existing direct path).
  - New `verify-place` rung (HEAVY; Makefile exclusive landing): the
    end-to-end accept leg (registry → verified placement → place-check), the
    differential anchor, and negative controls EVERY loop on mutated registry
    copies: unsigned (sig stripped), bad signature (statement forged),
    digest mismatch (blob/manifest tampered) — placer must refuse each
    BEFORE placing anything (staging dirs empty), with its own reason.
    Also the S1-deferred control: a crafted legacy image whose embedded
    identity carries image-digest= must be rejected (the §2.7 self-reference
    guard).

### S2 probe record (2026-06-11) — vehicle DECIDED: signify

Criterion (stated above, before probing): offline-buildable from the pinned
channel, sane derivation count, usable from the guix-free placer sandbox.

- **signify@32** (pinned channel, `gnu/packages/crypto.scm`): 3 drvs to build
  (2 fixed-output source fetches + 1 build; libbsd already in the closure).
  Built clean with `--no-substitutes` against the pin.
- **minisign@0.12**: 4 drvs (libsodium 1.0.18 + checkout + build). Also builds
  clean — viable fallback, strictly heavier.
- Functional smoke (placer-shaped flow): `signify -G -n` keygen, `-S` detached
  signature over a digest file, `-V` verifies; tampering one hex nibble of the
  digest fails closed with exit 1. Single binary, POSIX-sh-friendly — fits the
  placer sandbox the same way veritysetup does for --mkfs.

**Adopted: signify** (lighter, no extra runtime dep, OpenBSD pedigree). Key
fixture convention: like tests/keys/td_test_ed25519 (the SSH boot-test
fixture), the M12 signing keypair will be a COMMITTED test fixture — a key
generated at build time would not be reproducible — clearly marked test-only,
authorized nowhere outside the loop's rungs.

### Exclusive-landing announcements (§7.3)

- S1 touches the shared `Makefile` (`place` rung: pass `TD_IMAGES`); later
  sub-tasks will add M12 rungs. Each lands as a small standalone increment;
  others rebase.

### Verified-red evidence

**S1 (2026-06-11, ×3):**

- **A — assertion before implementation.** New place-check leg ran against the
  unmodified placer: `FAIL: generation 1: placed td-identity carries no
  well-formed image-digest=sha256:<64-hex> line (got #f) — the §2.7 identity
  anchor is missing` (and gen 2); rung exit non-zero. Then the placer change →
  green.
- **B — wrong content hashed.** Placer defect: `sha256sum "$work/manifest.json"`
  instead of the artifact. Equality leg caught it: `FAIL: generation 1:
  recorded image-digest "sha256:4103…3517" != sha256 of the placed artifact
  /gnu/store/hpv9…-td-generation-image-gen-1 (sha256:75d9…c12b) — the placer
  did not hash what it unpacked` (and gen 2); restored from the green commit.
- **C — shadowing/duplicate line.** Placer defect: the append run twice.
  Uniqueness leg caught it: `FAIL: generation 1: placed td-identity carries 2
  image-digest lines, expected exactly one` (and gen 2); restored.

Deferred red (noted, not skipped): the step-2 guard rejecting an EMBEDDED
`image-digest=` line (a forged self-digest) needs a crafted-image fixture to
fire behaviorally — S4 builds tampered-image fixtures anyway; exercise it
there.

**S3 (2026-06-12, ×4):**

- **Organic red — unsigned registry.** The full rung (signature legs included)
  ran against a td-registry that wrote statements but did NOT sign:
  `FAIL: generation 1: registry does not verify for sha256:6e18…884d: / no
  signature for …` (and gen 2; n2/n3 also red for reason-mismatch — the
  controls insist on their own reasons). Adding the signify -S invocation →
  green, including `guix build --check` (deterministic ed25519 confirmed).
- **Meta-reds ×3 — each negative control's mutation skipped** (good copy fed
  as the "bad" one, oci-load control-leg style), one run: `FAIL: negative
  control n1: an UNSIGNED registry was accepted`, `… n2: a TAMPERED blob
  (bca8…6316) was accepted`, `… n3: a FORGED statement was accepted`;
  restored from the green commit.

Review round (sub-agent): n2 victim selection moved from globally-largest
blob to the walked manifest's OWN largest ref — the gens share no blobs and
the two userspace layers differ by 12 bytes, so a future size flip would have
tampered a blob the gen-1 walk never visits (false red, not vacuous green);
rung-count comment 22→23.

**S4 (2026-06-12, ×6):**

- **Organic red — placer lacks verified mode.** The complete `verify-place`
  rung ran against the unmodified placer: the verified-tree derivation failed
  with `td-place: unknown argument: --registry` (exit 2), rung red. Then the
  verified-mode implementation → green end-to-end (accept leg + `--check` +
  digest-form place-check + differential + n1-n4).
- **Meta-reds ×5, one run** (mutation skipped / oracle fed as subject):
  differential with `vplace=$direct` → `FAIL: differential: …td-identity
  image-digest did not change representation` (×2, one per generation); n1
  stripping skipped → `the placer ACCEPTED an unsigned registry`; n2 forging
  skipped → `ACCEPTED a forged statement`; n3 corruption skipped → `ACCEPTED
  a tampered blob`; n4 injection skipped → `crafted image refused, but not by
  the self-reference guard` (the placer placed the well-formed crafted image,
  caught by the leg). Restored from the green commit.

The S1-deferred embedded-identity guard red is now PERMANENT: n4 crafts the
forged image and asserts the §2.7 rejection every loop.

Review round (sub-agent, no blockers): mode-appropriate refusal message
(empty $img in verified mode), two stale comments, dead run-counter dropped.
Noted, accepted: verified mode + --mkfs is an untested combination — the
verification gate is mode-independent (fires before any staging) and the
downstream mkfs path is shared with legacy --mkfs (covered by the place and
rollback rungs); a verified-mkfs leg can join if a future track distributes
mkfs placements.

### Finding: DIGESTS.md §2.7 manifest-digest record is stale (flagged)

The registry derives gen-1 manifest digest `sha256:6e18463d…` from today's
gen-1 image (`hpv950x5…`), but DIGESTS.md records `sha256:4076bfe6…` against
the older output `xmgdwy34…` — M11's sealing landings (a67b1c9, e58d8cc:
tmpfs-root + dm-verity in td-typed/td-verity) changed the generation images
after oci-load recorded its digests the same day. No rung asserts the
recorded VALUE (oci-load asserts a digest exists), so nothing is red; the
record is simply stale, like the store-path drift DIGESTS.md already flags.
Refreshing it is a deliberate DIGESTS.md re-baseline = exclusive landing
(§7.3) in a small standalone PR — M12 signs these digests, so doing that
refresh (recording 6e18463d…/gen-1, 3c6036fc…/gen-2 as the §2.7 identity at
the current baseline) falls naturally to this track; announced here.
