# PLAN.md — track status index

Scope contract: the approved roadmap, `DESIGN.md` §7.1. This file is ONLY the status
index — one line per track. **The track table below is GENERATED** from the per-track
records in `plan/tracks/<track>.md` by `tools/plan-index.sh`; do not hand-edit between
the markers. Per-track working notes: `plan/<track>.md` (single writer — the claiming
agent). Completed milestones: `HISTORY.md`. Reproducibility digests: `DIGESTS.md`.
Parallel-work rules: `CLAUDE.md` "Parallel work" / DESIGN §7.2–7.4.

Claim a track by adding (or editing) ITS record `plan/tracks/<track>.md` —
`status: claimed` + your handle + date — then run `tools/plan-index.sh` to refresh the
table and open a draft PR (main is branch-protected — no direct pushes; DESIGN §7.2).
Because the record is your OWN file, concurrent claims never collide on a shared line.
Flip `status:` to `done`/`closed` when you land (and re-run the renderer); the
`plan-index` gate fails the loop if PLAN.md drifts from the records, so a merged track
cannot keep reading "claimed". Handles are session-unique — generation mechanics in
`CLAUDE.md` "Parallel work". One agent per track; release the claim when you land or
stop. Claim status = the records on main plus the open PRs' claim edits.

<!-- BEGIN GENERATED TRACKS -->
<!-- Generated by tools/plan-index.sh from plan/tracks/*.md — do NOT edit by hand. -->

## Mainline (serial — one agent drives it at a time)

- [x] **M10.3 manual rollback + declared persistence** — DONE 2026-06-10 (claude-fable-9cb426) — manual rollback + declared persistence; review round done. — `plan/m10.md`
- [x] **M11 verified generations** — DONE 2026-06-11 (claude-fable-7d8371) — rollback gate grown to 36 asserts across three boots — sealed tmpfs-root + dm-verity store, corrupted root fails closed. — `plan/m11.md`
- [x] **M12 signed distribution** — DONE 2026-06-12 (claude-fable-c4148a) — registry gate signs OCI images + verify-place places only what verifies; DIGESTS §2.7 re-baselined. — `plan/m12.md`

## Side-tracks (parallel-safe)

- [x] **loop-latency** — DONE 2026-06-10 (claude-fable) — full check 525s→275s; new reset gate. — `plan/loop-latency.md`
- [x] **oci-load** — DONE 2026-06-11 (claude-fable-a03d13) — oci-load gate — skopeo foreign-loads the images into canonical OCI layouts, rejects a corrupted layer. — `plan/oci-load.md`
- [x] **offline-isolation** — CLOSED 2026-06-11 (claude-fable-cebe98) — undeclared-fetch-fails offline gate landed; daemon-side isolation rescoped to the own-builder era (human sign-off). — `plan/offline-isolation.md`
- [x] **rootless-builder** — DONE 2026-06-11 (claude-fable-ca67ec) — rootless gate — unprivileged userns daemon rebuilds the qcow2 image NAR-hash-equal to the root daemon. — `plan/rootless-builder.md`
- [x] **td-builder** — DONE 2026-06-11 (claude-fable-8ebb4e) — td-builder rebuilds the system qcow2 image drv daemon-equal (path/NAR/size/refs/deriver). — `plan/td-builder.md`
- [x] **check-memo** — DONE 2026-06-12 (claude-fable-580472) — verdict memoization on the --check legs; unchanged-tree floor 440s→145s; permanent memo gate. — `plan/check-memo.md`
- [x] **ci-gate** — DONE 2026-06-12 (claude-fable-52ceb1) — hosted-runner gate — unmodified ./check.sh fed by the CI store image (now the fast tier per #26). — `plan/ci-gate.md`
- [x] **ci-image-pipeline** — DONE 2026-06-13 (claude-fable-52ceb1, PR #14) — workflow builds/pushes/validates the CI store image, retags on main only; green end to end. — `plan/ci-gate.md`
- [x] **corpus-independence** — DONE 2026-06-13 (claude-fable-4a2e33, PR #21) — TS-authored hello/nano recipes converge on the corpus oracle; td's Rust builder replaces gnu-build-system (corpus/corpus-deps/td-build/td-build-deps gates). — `plan/corpus-independence.md`
- [x] **evaluator-as-library** — DONE 2026-06-13 (claude-fable-4a2e33, PR #22) — td constructs the .drv in Rust byte-identical to guix (drv-emit gate). — `plan/evaluator-as-library.md`
- [x] **fhs-app-images** — DONE 2026-06-13 (claude-fable-aed5c2) — container gate gains an FHS-layout app image; crun execs /usr/bin/hello via the in-image symlink, fails on the plain rootfs. — `plan/fhs-app-images.md`
- [x] **td-check** — DONE 2026-06-13 (claude-fable-4a2e33, PR #29) — td owns the reproducibility oracle — double-build + NAR compare, differential-checked against guix build --check (td-check gate). — `plan/td-check.md`
- [x] **td-drv-add** — DONE 2026-06-13 (claude-fable-4a2e33, PR #27) — td registers its .drv via the daemon's addTextToStore — no guile (derivation …) (drv-add gate). — `plan/td-drv-add.md`
- [x] **td-drv-assemble** — DONE 2026-06-13 (claude-fable-4a2e33, PR #28) — td assembles the .drv in Rust byte-identical to guix; removes the last guile (derivation …). — `plan/td-drv-assemble.md`
- [x] **td-drv-build** — DONE 2026-06-13 (claude-fable-4a2e33, PR #25) — td emits AND executes the .drv in its own userns sandbox, output NAR-equal to the daemon (td-drv-build gate). — `plan/td-drv-build.md`
- [x] **ts-frontend** — DONE 2026-06-13 (claude-fable-3ca5dd, PR #18) — TypeScript spec surface (ts/ts-eval/ts-diff gates) lowers store-path-equal to the frozen oracle; pure-Rust boa evaluator. — `plan/ts-frontend.md`
- [x] **gates-split** — DONE 2026-06-14 (claude-fable-2df2c3) — per-gate mk/gates/*.mk drop-in fragments replace the shared HEAVY_RUNGS line; renames rung→gate. Exclusive landing. — (infra; no track file)
- [ ] **honesty-fixes** — claimed claude-opus-117569 2026-06-14 — loop-sandbox review fixes (reap-on-death + minimal /dev + cgroup fail-closed + scratch cleanup + rootless single-target/comment + CI contract) and a generated/enforced PLAN.md (plan-index + sandbox-hardening gates). — `plan/honesty-fixes.md`
- [x] **input-resolution** — DONE 2026-06-14 (claude-fable-44df36, PR #45) — td-builder resolve reads a pinned input lock (no Guile), store-path-equal to Guile's resolution; td-build-resolved swaps the nano build onto it (resolve/td-build-resolved gates). — `plan/input-resolution.md`
- [x] **loop-sandbox** — DONE 2026-06-14 (claude-fable-4a2e33, PR #46) — td's sandbox is the SOLE loop container — no guix-shell-C fallback/toggle/carve-out; rootless nests via the PID-ns keystone. — `plan/loop-sandbox.md`
- [x] **td-store-db** — DONE 2026-06-14 (claude-fable-4a2e33, PR #50) — td owns the store SQLite DB authority (register/query/add/closure/verify/gc-sweep in pure Rust); store-backend gate holds + serves hello's output, no daemon. — `plan/td-store-db.md`
<!-- END GENERATED TRACKS -->

## The loop (reminder)

One command: `./check.sh`. The gate fragments under `mk/gates/*.mk` are the
authoritative gate list (each self-registers into the `CHEAP_GATES`/`HEAVY_GATES`
pool the `check:` target expands; `make list-gates` prints them — don't restate them
here); the cheap gates run serial-first, the heavy gates two at a time (`make -j2`),
and a red still short-circuits. Adding a gate is a new file, not an edit to a shared
list. Don't advance a sub-task until green. Small commits, each stating which test
now passes.
